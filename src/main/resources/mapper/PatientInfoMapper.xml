<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rehabilitation.mapper.PatientInfoMapper">

    <resultMap id="BaseResultMap" type="com.rehabilitation.entity.PatientInfo">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="gender" property="gender"/>
        <result column="birth_date" property="birthDate"/>
        <result column="id_card" property="idCard"/>
        <result column="phone" property="phone"/>
        <result column="medical_record_no" property="medicalRecordNo"/>
        <result column="disease_type" property="diseaseType"/>
        <result column="enrollment_institution" property="enrollmentInstitution"/>
        <result column="address" property="address"/>
        <result column="created_time" property="createdTime"/>
        <result column="updated_time" property="updatedTime"/>
    </resultMap>

    <select id="selectPatientInfoPage" resultMap="BaseResultMap">
        SELECT * FROM patient
        <where>
            <if test="name != null and name != ''">
                AND (name LIKE CONCAT('%', #{name}, '%') OR phone LIKE CONCAT('%', #{name}, '%'))
            </if>
            <if test="phone != null and phone != '' and phone != name">
                AND phone LIKE CONCAT('%', #{phone}, '%')
            </if>
            <if test="startTime != null">
                AND created_time >= #{startTime}
            </if>
            <if test="endTime != null">
                AND created_time &lt;= #{endTime}
            </if>
        </where>
        ORDER BY created_time DESC
    </select>

</mapper>