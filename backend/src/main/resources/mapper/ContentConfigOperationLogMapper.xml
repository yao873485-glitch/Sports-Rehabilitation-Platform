<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rehabilitation.mapper.ContentConfigOperationLogMapper">

    <select id="selectByContentIdAndType" resultType="com.rehabilitation.entity.ContentConfigOperationLog">
        SELECT
            id,
            content_id,
            content_type,
            operation_type,
            operation_content,
            operator,
            operation_time
        FROM content_config_operation_log
        WHERE content_id = #{contentId}
          AND content_type = #{contentType}
        ORDER BY operation_time DESC
    </select>

</mapper>
